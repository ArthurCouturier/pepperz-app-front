function Footer({className}: HeaderProps) {
    return (
        <>
            <div className={`h-[10vh] flex items-center justify-center ` + className}>
                Footer
            </div>
        </>
    )
}

export default Footer;
